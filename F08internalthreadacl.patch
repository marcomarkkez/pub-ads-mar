diff --git a/.claude/todos/mvp-sprint.json b/.claude/todos/mvp-sprint.json
index e2f8c14..6a17200 100644
--- a/.claude/todos/mvp-sprint.json
+++ b/.claude/todos/mvp-sprint.json
@@ -42,7 +42,7 @@
       { "id": "F05", "title": "Client uploads THEIR creative per ad (image/video/audio) vs space media-specs; client never creates spaces/ads; installer subrole uploads proofs", "design_section": "5, 7, 19-case4", "checklist": [], "mvp": true, "status": "resolved", "note": "Spec confirmed correct in design.md + new actor-boundary case (19 journey 4). CODE GAP: remove the '+ Ad Space' create-from-scratch form; per-ad action = upload creative to the existing (rented-space) ad. Impl pending owner go-ahead." },
       { "id": "F06", "title": "Booking action & per-ad go-live (provider Approve->installator / Reject; optional reason; no counter-offers)", "design_section": "5, 20", "checklist": ["C13"], "mvp": true, "status": "resolved", "note": "Owner-confirmed 2026-07-10. Approve/Reject+optional reason: backend done; ADDED the provider-UI Reject flow with optional reason (was Approve/Cancel only). Per-ad go-live inherent (bookings carry ad_id). DEFERRED to their features: installation queue (install_date + installator view) -> F07; inquiry->booking thread on approval+payment -> F08." },
       { "id": "F07", "title": "Proof of display (provider/installator/supervisor upload primary proof; client accept/reject; deadline; NO re-upload loop -> reject auto-holds + auto-routes to Payments+Support, manual human release)", "design_section": "7", "checklist": ["C07", "C09"], "mvp": true, "status": "spec_resolved", "note": "Owner 2026-07-10: NO re-upload cycle. Client reject -> ONLY automatic = payout auto-hold + auto-route ONE ticket to Payments+Support (payment attached). Release/refund is a MANUAL Payments+Support human decision. Primary-proof uploaders = provider owner OR Installator OR Supervisor subrole (NOT Sales, no client role); 'manager' interpreted as provider Supervisor (confirm). design.md reconciled (removed 48h re-upload window in ~8 places). CODE PENDING: verify/enforce uploader roles; remove any re-upload UI; ensure reject -> auto-hold + auto dual-ticket + no auto-release." },
-      { "id": "F08", "title": "Chat & PII masking (contact masked; booked-space address whitelisted)", "design_section": "10", "checklist": ["C06"], "mvp": true, "status": "pending" },
+      { "id": "F08", "title": "Chat & PII masking (contact masked; booked-space address whitelisted; internal Support<->Payments threads client-invisible)", "design_section": "10", "checklist": ["C06"], "mvp": true, "status": "resolved", "note": "Owner-approved 2026-07-10. Masking core done (phone/email/URL/address + booked-space whitelist + privileged roles see raw). ADDED: explicit internal-thread ACL in MessageController (non-staff blocked from type=internal on read AND post) + Feature test InternalThreadAclTest (spec-requested: client 403, payments 200) - run in Codespace (php artisan test). DEFERRED to F09/F11: Support/Payments JOIN flow (support_joined_at never set yet; masking already relaxes on it) + Payments-join relax. Polymorphic conversation reconciliation = AFTER-MVP." },
       { "id": "F09", "title": "Tickets & support (Support button on any object; auto-ticket on proof mismatch)", "design_section": "11", "checklist": ["C04", "C08", "C09"], "mvp": true, "status": "pending" },
       { "id": "F10", "title": "Money (Payments payouts/refunds, wallet, per-provider payout, per-ad invoice lines)", "design_section": "8", "checklist": ["C10"], "mvp": true, "status": "pending" },
       { "id": "F11", "title": "Admin oversight / eagle-eye (sees all incl. support & payments chats)", "design_section": "12", "checklist": ["C11"], "mvp": true, "status": "pending" },
diff --git a/backend/app/Http/Controllers/Shared/MessageController.php b/backend/app/Http/Controllers/Shared/MessageController.php
index 47e92c7..d5830d4 100644
--- a/backend/app/Http/Controllers/Shared/MessageController.php
+++ b/backend/app/Http/Controllers/Shared/MessageController.php
@@ -10,11 +10,27 @@
 
 class MessageController extends Controller
 {
+    // design.md §10 [F08]: internal Support↔Payments threads are never visible to
+    // clients/providers — membership-based ACL, not a flag on a shared thread.
+    private const STAFF_ROLES = ['admin', 'support', 'payments'];
+
+    private function forbidsAccess(Conversation $conversation, $user): bool
+    {
+        // Internal threads: only staff (admin/support/payments) may read/post.
+        if (($conversation->type ?? null) === 'internal') {
+            return !in_array($user->role, self::STAFF_ROLES, true);
+        }
+
+        // Client↔provider threads: only the two participants.
+        return $conversation->client_user_id !== $user->id
+            && $conversation->provider_user_id !== $user->id;
+    }
+
     public function index(Request $request, Conversation $conversation): JsonResponse
     {
         $user = $request->user();
 
-        if ($conversation->client_user_id !== $user->id && $conversation->provider_user_id !== $user->id) {
+        if ($this->forbidsAccess($conversation, $user)) {
             return response()->json(['message' => 'Forbidden.'], 403);
         }
 
@@ -46,7 +62,7 @@ public function store(Request $request, Conversation $conversation): JsonRespons
     {
         $user = $request->user();
 
-        if ($conversation->client_user_id !== $user->id && $conversation->provider_user_id !== $user->id) {
+        if ($this->forbidsAccess($conversation, $user)) {
             return response()->json(['message' => 'Forbidden.'], 403);
         }
 
diff --git a/backend/tests/Feature/InternalThreadAclTest.php b/backend/tests/Feature/InternalThreadAclTest.php
new file mode 100644
index 0000000..1b88a5b
--- /dev/null
+++ b/backend/tests/Feature/InternalThreadAclTest.php
@@ -0,0 +1,89 @@
+<?php
+
+namespace Tests\Feature;
+
+use App\Models\Conversation;
+use App\Models\Space;
+use App\Models\User;
+use Database\Seeders\RolePermissionSeeder;
+use Illuminate\Foundation\Testing\RefreshDatabase;
+use Laravel\Sanctum\Sanctum;
+use Tests\TestCase;
+
+/**
+ * design.md §10 [F08]: internal Support↔Payments threads use a membership-based
+ * ACL — a client can NEVER fetch an internal-thread message. This asserts it.
+ */
+class InternalThreadAclTest extends TestCase
+{
+    use RefreshDatabase;
+
+    public function test_client_cannot_fetch_internal_thread_messages(): void
+    {
+        $this->seed(RolePermissionSeeder::class);
+
+        $client = User::factory()->create(['role' => 'client']);
+        $provider = User::factory()->create(['role' => 'provider']);
+
+        $space = Space::create([
+            'user_id' => $provider->id,
+            'name' => 'Test Space',
+            'latitude' => 25.6597,
+            'longitude' => -100.4023,
+        ]);
+
+        // An internal thread — even though the client is on the client_user_id
+        // column, the internal type must block them.
+        $internal = Conversation::create([
+            'space_id' => $space->id,
+            'client_user_id' => $client->id,
+            'provider_user_id' => $provider->id,
+            'type' => 'internal',
+        ]);
+        $internal->messages()->create([
+            'sender_user_id' => $provider->id,
+            'body' => 'internal staff-only note',
+        ]);
+
+        Sanctum::actingAs($client);
+
+        $this->getJson("/api/conversations/{$internal->id}/messages")
+            ->assertStatus(403);
+
+        $this->postJson("/api/conversations/{$internal->id}/messages", ['body' => 'hi'])
+            ->assertStatus(403);
+    }
+
+    public function test_payments_staff_can_fetch_internal_thread_messages(): void
+    {
+        $this->seed(RolePermissionSeeder::class);
+
+        $client = User::factory()->create(['role' => 'client']);
+        $provider = User::factory()->create(['role' => 'provider']);
+        $payments = User::factory()->create(['role' => 'payments']);
+
+        $space = Space::create([
+            'user_id' => $provider->id,
+            'name' => 'Test Space',
+            'latitude' => 25.6597,
+            'longitude' => -100.4023,
+        ]);
+
+        $internal = Conversation::create([
+            'space_id' => $space->id,
+            'client_user_id' => $client->id,
+            'provider_user_id' => $provider->id,
+            'type' => 'internal',
+        ]);
+        $internal->messages()->create([
+            'sender_user_id' => $provider->id,
+            'body' => 'internal staff-only note',
+        ]);
+
+        Sanctum::actingAs($payments);
+
+        $this->getJson("/api/conversations/{$internal->id}/messages")
+            ->assertStatus(200)
+            ->assertJsonPath('data.messages.0.body', 'internal staff-only note');
+    }
+}
