diff --git a/backend/.env.example b/backend/.env.example
index 9e425db..89fa442 100644
--- a/backend/.env.example
+++ b/backend/.env.example
@@ -33,6 +33,11 @@ SESSION_ENCRYPT=false
 SESSION_PATH=/
 SESSION_DOMAIN=null
 
+# Comma-separated extra allowed frontend origins for CORS, on top of
+# http://localhost:4200. Codespaces URLs (*.app.github.dev) are matched
+# automatically via config/cors.php and don't need to be listed here.
+CORS_ALLOWED_ORIGINS=http://localhost:4200
+
 BROADCAST_CONNECTION=log
 FILESYSTEM_DISK=local
 QUEUE_CONNECTION=database
diff --git a/backend/config/cors.php b/backend/config/cors.php
index 84f7e0b..bd44607 100644
--- a/backend/config/cors.php
+++ b/backend/config/cors.php
@@ -19,9 +19,14 @@
 
     'allowed_methods' => ['*'],
 
-    'allowed_origins' => ['http://localhost:4200'],
-
-    'allowed_origins_patterns' => [],
+    'allowed_origins' => array_filter(explode(',', env('CORS_ALLOWED_ORIGINS', 'http://localhost:4200'))),
+
+    // Matches GitHub Codespaces forwarded-port URLs, e.g.
+    // https://<codespace-name>-4200.app.github.dev — the dev environment
+    // (see claude.md → GitHub Codespaces).
+    'allowed_origins_patterns' => [
+        '#^https://.*\.(app\.github\.dev|githubpreview\.dev)$#',
+    ],
 
     'allowed_headers' => ['*'],
 
diff --git a/backend/docker/entrypoint.sh b/backend/docker/entrypoint.sh
index feee271..b2bf0eb 100644
--- a/backend/docker/entrypoint.sh
+++ b/backend/docker/entrypoint.sh
@@ -3,6 +3,13 @@ set -e
 
 cd /var/www
 
+# Remove stale bootstrap caches BEFORE any artisan command. A leftover
+# packages.php/services.php from a previous checkout can reference a
+# ServiceProvider that isn't installed yet and crash the framework on boot —
+# and because every artisan call boots the framework first, it can't clear
+# itself out of the loop. Deleting the files forces fresh package discovery.
+rm -f bootstrap/cache/packages.php bootstrap/cache/services.php bootstrap/cache/config.php
+
 if [ ! -f .env ]; then
     cp .env.example .env
     php artisan key:generate --force
diff --git a/backend/docker/nginx.conf b/backend/docker/nginx.conf
index 03c7787..1e795c1 100644
--- a/backend/docker/nginx.conf
+++ b/backend/docker/nginx.conf
@@ -20,7 +20,14 @@ server {
     error_page 404 /index.php;
 
     location ~ \.php$ {
-        fastcgi_pass backend:9000;
+        # Resolve the backend hostname per-request via Docker's embedded DNS
+        # (127.0.0.11) instead of once at startup. Using a variable for
+        # fastcgi_pass forces runtime resolution, so nginx no longer dies with
+        # "host not found in upstream" if the backend container isn't up yet.
+        resolver 127.0.0.11 valid=10s ipv6=off;
+        set $upstream_backend backend:9000;
+
+        fastcgi_pass $upstream_backend;
         fastcgi_index index.php;
         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
         include fastcgi_params;
diff --git a/frontend/src/environments/environment.ts b/frontend/src/environments/environment.ts
index 7d5ecf2..9e9b2de 100644
--- a/frontend/src/environments/environment.ts
+++ b/frontend/src/environments/environment.ts
@@ -1,6 +1,21 @@
+// Resolves the API origin from the current browser location so the same build
+// works on plain localhost and on GitHub Codespaces (which serves the frontend
+// and backend on different -PORT subdomains). See claude.md → GitHub Codespaces.
+function resolveApiUrl(): string {
+  if (typeof window === 'undefined') {
+    return 'http://localhost:8000/api';
+  }
+  const { hostname, protocol } = window.location;
+  const codespacesMatch = hostname.match(/^(.*)-4200\.(app\.github\.dev|githubpreview\.dev)$/);
+  if (codespacesMatch) {
+    return `${protocol}//${codespacesMatch[1]}-8000.${codespacesMatch[2]}/api`;
+  }
+  return 'http://localhost:8000/api';
+}
+
 export const environment = {
   production: false,
-  apiUrl: 'http://localhost:8000/api',
+  apiUrl: resolveApiUrl(),
   // Map provider: 'leaflet' = Leaflet + OpenStreetMap (current)
   // Switch to 'google' to use Google Maps (set googleMapsApiKey too)
   mapProvider: 'leaflet' as 'leaflet' | 'google',
