diff --git a/.claude/todos/mvp-sprint.json b/.claude/todos/mvp-sprint.json
index dce061f..ad3798e 100644
--- a/.claude/todos/mvp-sprint.json
+++ b/.claude/todos/mvp-sprint.json
@@ -31,6 +31,29 @@
     { "id": "C12", "item": "Admin configurations; apply-scope new-only vs all objects", "status": "implemented", "note": "ConfigurationController apply_scope new_only|all + in-flight snapshot; not UI-walked" },
     { "id": "C13", "item": "End-to-end client journey (map-select -> campaign -> adset -> book/refund -> support chat)", "status": "verified", "note": "core chain runtime-verified; provider-reject/auto-reject/proof legs are implemented, partial UI walk pending" }
   ],
+  "feature_review": {
+    "method": "See claude.md -> Working Method. One feature at a time: doubts/clashes -> ask owner; else resolve with MVP common sense, mark status, report next feature. design.md is the single spec.",
+    "status_legend": { "pending": "not yet reviewed", "reviewing": "under review now", "needs_decision": "blocked on owner decision", "resolved": "spec clear + marked; code may still be pending", "deferred": "AFTER-MVP" },
+    "features": [
+      { "id": "F01", "title": "Auth & 5 roles (client, provider, support, payments, admin)", "design_section": "1, 14", "checklist": ["C01"], "mvp": true, "status": "resolved", "note": "spec clear, verified (C01)" },
+      { "id": "F02", "title": "Accounts & collaborators (subroles: client publicist/manager; provider installator/sales/supervisor; never billing)", "design_section": "3", "checklist": ["C02"], "mvp": true, "status": "resolved", "note": "spec clear (design.md 3), verified (C02)" },
+      { "id": "F03", "title": "Catalog & map search (3-pane menu|map|list, nearest-first, radio-in-map-range, exact dates)", "design_section": "5", "checklist": ["C03"], "mvp": true, "status": "resolved", "note": "spec clear; O11 unavailable-sorted-to-bottom added. CODE GAP: mvp-build space-search is a map/list TOGGLE, not the simultaneous 3-pane (design.md 5) -> impl pending" },
+      { "id": "F04", "title": "Campaign->adset->ad backlog (orphans; move to new OR existing adset; ad-origin invariant)", "design_section": "4, 5", "checklist": ["C03", "C13"], "mvp": true, "status": "resolved", "note": "orphans move to new/existing adset (done); ad-origin invariant documented" },
+      { "id": "F05", "title": "Client uploads THEIR creative per ad (image/video/audio) vs space media-specs; client never creates spaces/ads; installer subrole uploads proofs", "design_section": "5, 7, 19-case4", "checklist": [], "mvp": true, "status": "resolved", "note": "Spec confirmed correct in design.md + new actor-boundary case (19 journey 4). CODE GAP: remove the '+ Ad Space' create-from-scratch form; per-ad action = upload creative to the existing (rented-space) ad. Impl pending owner go-ahead." },
+      { "id": "F06", "title": "Booking action & per-ad go-live (provider Approve->installator / Reject; optional reason; no counter-offers)", "design_section": "5, 20", "checklist": ["C13"], "mvp": true, "status": "pending" },
+      { "id": "F07", "title": "Proof of display (installator uploads; client accept/reject; deadline; reject->re-upload loop)", "design_section": "7", "checklist": ["C07", "C09"], "mvp": true, "status": "pending" },
+      { "id": "F08", "title": "Chat & PII masking (contact masked; booked-space address whitelisted)", "design_section": "10", "checklist": ["C06"], "mvp": true, "status": "pending" },
+      { "id": "F09", "title": "Tickets & support (Support button on any object; auto-ticket on proof mismatch)", "design_section": "11", "checklist": ["C04", "C08", "C09"], "mvp": true, "status": "pending" },
+      { "id": "F10", "title": "Money (Payments payouts/refunds, wallet, per-provider payout, per-ad invoice lines)", "design_section": "8", "checklist": ["C10"], "mvp": true, "status": "pending" },
+      { "id": "F11", "title": "Admin oversight / eagle-eye (sees all incl. support & payments chats)", "design_section": "12", "checklist": ["C11"], "mvp": true, "status": "pending" },
+      { "id": "F12", "title": "Admin configurations (apply-scope new-only vs all; snapshot-at-creation)", "design_section": "12", "checklist": ["C12"], "mvp": true, "status": "pending" },
+      { "id": "F13", "title": "Notifications & calendar (availability, iCal/Google sync, date ranges)", "design_section": "13", "checklist": ["C05"], "mvp": true, "status": "pending" },
+      { "id": "F14", "title": "RBAC & capabilities (role_permissions, per-route middleware, permissions editor)", "design_section": "14", "checklist": [], "mvp": true, "status": "pending" },
+      { "id": "F15", "title": "Book-for-later, pre-pay & escrow", "design_section": "6", "checklist": [], "mvp": false, "status": "deferred" },
+      { "id": "F16", "title": "Strikes (90-day counter, appeals)", "design_section": "7", "checklist": [], "mvp": false, "status": "deferred" },
+      { "id": "F17", "title": "Ratings", "design_section": "9", "checklist": [], "mvp": false, "status": "deferred" }
+    ]
+  },
   "workstreams": [
     { "id": "WS-beads", "title": "Diagnose + restore beads (dolt->sqlite)", "status": "done" },
     { "id": "WS-audit", "title": "Audit 13 checklist items + doc state vs code", "status": "done" },
diff --git a/claude.md b/claude.md
index 1a265e8..366888f 100644
--- a/claude.md
+++ b/claude.md
@@ -25,6 +25,25 @@ Web-based advertising spaces marketplace built with **Laravel 12** (API backend)
   is printed in the Claude UI chat, NOT written to a file, unless the owner explicitly asks for a
   file. Temporary working files must be folded into design.md/todos and deleted once consumed.
 
+## Working Method — feature-by-feature MVP review (owner rule, 2026-07-10)
+
+Too much time was lost on definitions that never got acted on. From now on we
+review the product **one feature at a time**, driving to a deployable MVP:
+
+- **Enumerate every feature** and keep them ordered in the todos JSON
+  (`.claude/todos/`) and mirrored to the relevant `design.md` section. `design.md`
+  is the single canonical spec (there is no ARCHITECTURE.md).
+- **MVP bias: functional over precise.** Shipping something that runs beats a
+  perfect spec. **No feature is in a client's or architect's request backlog**,
+  so we are free to decide. If a feature question is simple, RESOLVE IT NOW with
+  common sense and record the decision — do not defer.
+- **Only escalate real forks:** ask the owner ONLY when a feature is genuinely
+  ambiguous or when two features clash with each other. Otherwise decide and move.
+- **Per feature:** if doubts/clashes → ask; else resolve + mark the feature's
+  status in the todos + update `design.md` if needed, then report the NEXT
+  feature in the list so the owner can review, correct, or skip to the next.
+- Latest owner instruction on a topic wins over any earlier spec.
+
 ## Key Files
 
 | File | Purpose |
diff --git a/design.md b/design.md
index b8f49b1..86bdd95 100644
--- a/design.md
+++ b/design.md
@@ -197,7 +197,8 @@ re-queries radio stations in range; pan/zoom updates the station list.
 Filters: space type, date range, budget per day. Dates are EXACT — there is NO date-flexibility
 (±X days) filter; clients/providers add slack via wider windows or "book for later."
 Availability turns green/red for the chosen date range. Spaces whose calendar doesn't coincide
-are TAGGED ("doesn't coincide with your timespan"), not hidden, and remain bookable "for later."
+are TAGGED ("doesn't coincide with your timespan"), not hidden, and remain bookable "for later";
+they are SORTED to the BOTTOM of the nearest-first list (available spaces first, unavailable last).
 The campaign view shows an Orphan-spaces list (backlog spaces not yet in any adset); a client
 MAY check out a partial campaign, but any orphaned ad cannot go live and its payment cannot be
 processed — orphans can be bulk-moved into an adset (either a NEW adset created
@@ -828,6 +829,20 @@ bump to 200 DPI. ² −14 LUFS aligns with common Mexican broadcast loudness; pr
    for blur → payment auto-held + ONE ticket to Support&Payments (masking relaxes when Support
    joins) → provider re-uploads → Support+Payments agree → payout released — all one chronological
    thread with system-transition messages.
+4. Ad creative & installation — WHO does WHAT (actor boundaries; INVARIANT, added to stop the
+   recurring "client creates a space/ad" drift):
+   - PROVIDER creates the space (billboard, photo/video panel, radio station) with its own
+     photos + media-delivery specs. A CLIENT never creates a space, never uploads a space photo,
+     and never sets media specs — that is exclusively the provider's job.
+   - CLIENT rents spaces from the map; each rented space becomes an ad in the campaign backlog.
+     The client's ONLY content action is uploading THEIR creative — the image, video, or audio
+     they want shown in that space — for each ad, validated against the space's media-delivery
+     specs. The client never mints a space-less ad (see §5 ad-origin invariant).
+   - PROVIDER approves the booking and dispatches it to an INSTALLATOR (a subrole employee under
+     the provider's own account).
+   - INSTALLATOR physically installs the ad and uploads the PROOF of display (photo, or video for
+     radio/audio), logged in with their installator subrole under the provider's account. Proofs
+     are a provider-side artifact; the client only ACCEPTS or REJECTS a proof, never uploads one.
 
 == 20. Roadmap, Revisions & Known Gaps ==
 
